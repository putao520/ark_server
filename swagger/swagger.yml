swagger: "2.0"
info:
  title: beego Test API
  description: beego has a very cool tools to autogenerate documents for your API
  version: 1.0.0
  termsOfService: http://beego.me/
  contact:
    email: astaxie@gmail.com
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
basePath: /v1
paths:
  /scriptLibrary/:
    get:
      tags:
      - scriptLibrary
      description: get ScriptLibrary
      operationId: ScriptLibraryController.Get All
      parameters:
      - in: query
        name: query
        description: Filter. e.g. col1:v1,col2:v2 ...
        type: string
      - in: query
        name: fields
        description: Fields returned. e.g. col1,col2 ...
        type: string
      - in: query
        name: sortby
        description: Sorted-by fields. e.g. col1,col2 ...
        type: string
      - in: query
        name: order
        description: Order corresponding to each sortby field, if single value, apply
          to all sortby fields. e.g. desc,asc ...
        type: string
      - in: query
        name: limit
        description: Limit the size of result set. Must be an integer
        type: string
      - in: query
        name: offset
        description: Start position of result set. Must be an integer
        type: string
      responses:
        "200":
          description: ""
          schema:
            $ref: '#/definitions/models.ScriptLibrary'
        "403":
          description: ""
    post:
      tags:
      - scriptLibrary
      description: create ScriptLibrary
      operationId: ScriptLibraryController.Post
      parameters:
      - in: body
        name: body
        description: body for ScriptLibrary content
        required: true
        schema:
          $ref: '#/definitions/models.ScriptLibrary'
      responses:
        "201":
          description: '{int} models.ScriptLibrary'
        "403":
          description: body is empty
  /scriptLibrary/{id}:
    get:
      tags:
      - scriptLibrary
      description: get ScriptLibrary by id
      operationId: ScriptLibraryController.Get One
      parameters:
      - in: path
        name: id
        description: The key for staticblock
        required: true
        type: string
      responses:
        "200":
          description: ""
          schema:
            $ref: '#/definitions/models.ScriptLibrary'
        "403":
          description: :id is empty
    put:
      tags:
      - scriptLibrary
      description: update the ScriptLibrary
      operationId: ScriptLibraryController.Put
      parameters:
      - in: path
        name: id
        description: The id you want to update
        required: true
        type: string
      - in: body
        name: body
        description: body for ScriptLibrary content
        required: true
        schema:
          $ref: '#/definitions/models.ScriptLibrary'
      responses:
        "200":
          description: ""
          schema:
            $ref: '#/definitions/models.ScriptLibrary'
        "403":
          description: :id is not int
    delete:
      tags:
      - scriptLibrary
      description: delete the ScriptLibrary
      operationId: ScriptLibraryController.Delete
      parameters:
      - in: path
        name: id
        description: The id you want to delete
        required: true
        type: string
      responses:
        "200":
          description: '{string} delete success!'
        "403":
          description: id is empty
definitions:
  models.Object:
    title: Object
    type: object
    properties:
      ObjectId:
        type: string
      PlayerName:
        type: string
      Score:
        type: integer
        format: int64
  models.Profile:
    title: Profile
    type: object
    properties:
      Address:
        type: string
      Age:
        type: integer
        format: int64
      Email:
        type: string
      Gender:
        type: string
  models.ScriptLibrary:
    title: ScriptLibrary
    type: object
    properties:
      CreateAt:
        description: 添加时间
        type: string
        format: datetime
      Creator:
        description: 添加人id
        type: integer
        format: int64
      Desc:
        description: 脚本说明
        type: string
      Id:
        type: integer
        format: int64
      Name:
        description: 脚本名称
        type: string
      Path:
        description: 脚本储存位置
        type: string
      UpdateAt:
        description: 更新时间
        type: string
        format: datetime
  models.User:
    title: User
    type: object
    properties:
      Id:
        type: string
      Password:
        type: string
      Profile:
        $ref: '#/definitions/models.Profile'
      Username:
        type: string
tags:
- name: scriptLibrary
  description: |2
     ScriptLibraryController operations for ScriptLibrary
